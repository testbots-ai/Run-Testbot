{
  "name": "testbot-action",
  "version": "1.0.0",
  "description": "GitHub Action to run AutomationHQ Test Bot with JWT authentication",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "prepare": "npm run bundle"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-fetch": "^2.6.11",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.5"
  }
}
